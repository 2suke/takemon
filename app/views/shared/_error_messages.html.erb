<% if @user.errors.any? %>
<ul class="alert alert-danger">
    <p>入力された値に以下<%= @user.errors.count %>件の問題がありました。</p>
    <% @user.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
</ul>
<% end %>