<h1>アカウント情報編集</h1>

<%= form_with(model: @user, local: true) do |form| %>

<%= render 'shared/error_messages', object: @user %>

<%= render  'users/core_info', object: form %>

<%= form.submit 'アカウント情報更新', class:"btn btn-primary" %>

<% end %>