<h1>ユーザー登録</h1>

<%= form_with(model: @user, local: true) do |form| %>

<%= render 'shared/error_messages', object: @user %>

<%= render  'users/core_info', form: form %>

<%= form.submit 'アカウント作成', class:"btn btn-primary" %>

<% end %>
