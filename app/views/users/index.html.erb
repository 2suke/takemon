<p><%= @users.count %>人のユーザーが登録されています。</p>
<ul>
    <% @users.each do |user| %>
    <li>
        <p>id: <%= user.id %></p>
        <p>name: <%= user.name %></p>
        <p>email: <%= user.email %></p>
    </li>
    <% end %>
</ul>